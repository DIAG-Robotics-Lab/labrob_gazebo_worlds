# labrob_gazebo_worlds

Collection of worlds for gazebo.

To use `gazebo-ros-actor-plugin` to command humans with a C++ script, you need to update the submodules
> [!NOTE]
> the plugin is downloading the code for `ROS noetic` from the homonym branch
```
git submodule update --recursive --init
```